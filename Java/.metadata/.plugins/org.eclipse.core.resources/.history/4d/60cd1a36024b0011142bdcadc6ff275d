package tp3;

import java.util.LinkedList;
import java.util.Queue;

public class RedAguaPotable {

	public double minimoCaudal(double caudal) {
		
	}
	
	private double contarCaudal(GeneralTree<Character> a, double caudal) {
		Queue<GeneralTree<Character>> cola = new LinkedList<GeneralTree<Character>>();
		if (a == null) {
			return -1;
		}
		int cantidadNivelActual = 0;
		double caudalMin = 9999;
		
		cola.add(a);
		
		while (!cola.isEmpty()) {
			cantidadNivelActual = cola.size();
			double caudalActual = cantidadNivelActual;
			if (caudalActual < caudalMin) {
				caudalMin = caudalActual;
			}
			
			for (int i = 0; i < cantidadNivelActual; i++) {
				GeneralTree<Character> actual = cola.poll();
				for (GeneralTree<Character> hijo: actual.getChildren()) {
					cola.add(hijo);
				}
			}
			
			nivelActual++;
		}
		return maxNivel;
	}
}
